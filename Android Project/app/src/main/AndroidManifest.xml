<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ronnie.formativeteams">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".CurrentAssessmentsActivity"
            android:label="@string/title_activity_current_assessments"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CompletedAssessmentsActivity"
            android:label="@string/title_activity_completed_assessments"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TeamLeaderboardActivity"
            android:label="@string/title_activity_team_leaderboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".TeacherMenuActivity" />
        <activity android:name=".TeamCreationActivity" />
        <activity android:name=".StudentCreationActivity" />
        <activity android:name=".CreateAssessmentActivity" />
        <activity android:name=".ViewCorrectAnswersActivity" />
        <activity android:name=".FeedbackListActivity"></activity>
    </application>

</manifest>